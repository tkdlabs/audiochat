[package]
name = "audiochat-stt-whisper"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
audiochat-core = { workspace = true }
whisper-rs = "0.16"

[dev-dependencies]
hound = "3.5"

[features]
default = []
# Build with CUDA support (requires CUDA toolkit).
cuda = ["whisper-rs/cuda"]
